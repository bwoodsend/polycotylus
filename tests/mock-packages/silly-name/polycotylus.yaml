source_top_level: something-unrelated-{version}

spdx:
  custom:

dependencies:
  build:
    arch manjaro fedora void alpine: croc
    python: sqlite3
    pip: pyperclip -r build-requirements.txt
  run:
    arch manjaro fedora void alpine: jq
    python: tkinter
    pip: TABULATE -r requirements.txt
  test:
    arch manjaro fedora void alpine: cmatrix
    python: sqlite3 tkinter
    pip: pytest[feet]

test_command: TEST_VARIABLE=hello pytest -k 'not unrunable' && python -c 'print("hello")'

test_files:
  - the_test_suite
  - pytest.ini
