source_url: https://github.com/bwoodsend/ubrotli/archive/refs/tags/{version}.tar.gz

spdx:
  Apache-2.0:

dependencies:
  build:
    arch: brotli
    alpine: brotli-dev python3-dev gcc musl-dev
    void: brotli-devel python3-devel
  run:
    arch: brotli
    alpine: brotli-libs
    void: brotli
  test:
    pip: pytest
